/**
 * Continuum Health brand tokens (Topic 2 lock).
 * Light mode only (Topic 7 lock).
 */
export const brandTeal = '#0E7C86'

export const theme = {
  brandTeal,
} as const

export type Theme = typeof theme
